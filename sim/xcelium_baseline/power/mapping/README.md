# Genus mapping input

Place the Genus RTL-to-gate mapping file for the currently checked
`netlist/fa_top_mapped.v` here:

```text
power/mapping/fa_top_genus_mapping.rpt
```

This file must be generated from the same Genus run as the netlist used for
power. Do not mix an old mapping file with a regenerated netlist.
